from board import Connect4Board
from turns import check_for_winner
import random

# This function should return the col of the next move
def next_move(board, current_player):
    move = random.randint(0,7)
    if check_for_winner == True:
        pass
    # elif opponent is about to win then block
    # elif your about to win put it in winning spot
    # elif random row is not full drop

    return 0
